#include <stdio.h>

void main() {
    int numbersArray[] = {1, 3, 5, 6, 7};
    int size = 5;
    int newArray[size];
    int x = size;

    for (int i = 0; i < size; ++i) {
        int toCompare = numbersArray[i];

        for (int j = i+1; j < x; ++j) {

        }
        x--;
    }
}
